<div class="navBar">
  <div class="navPath">
    <% if user_signed_in? %>
      <%= link_to "My Files", home_index_path %>
      <%= link_to "Upload New File", new_excel_sheet_path%>
    <% end %>
  </div>
  <div class="authLinks">
    <% if current_user.nil?%>
      <%= link_to "Log In", new_user_session_path%>
    <% else %>
      <p>Welcome, <%= current_user.email%></p>
      <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
    <% end %>
  </div>
</div>
